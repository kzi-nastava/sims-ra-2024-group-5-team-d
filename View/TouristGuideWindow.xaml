<Window x:Class="BookingApp.View.TouristGuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="TouristGuideWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Click="CreateNewTourWindow" Grid.Row="0" Grid.Column="0" Content="Create new Tour"/>
        <Button Click="CreateNewTourRealisationWindow" Grid.Row="0" Grid.Column="1" Content="Create new Tour Realisation"/>
        <Button  Grid.Column="2" Grid.Row="0" Content="Show stats"/>
        <TabControl Grid.Row="1" Grid.ColumnSpan="3">
            
            <!-- First Tab -->
            <TabItem Header="All Tours">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="1" 
                              Grid.ColumnSpan="2" 
                              ItemsSource="{Binding AllTours,UpdateSourceTrigger=PropertyChanged}" 
                              SelectedItem="{Binding SelectedTour}"
                              AutoGenerateColumns="False" 
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="*" Header="Picture">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImagesPath}" Width="50" Height="50"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Width="*" Header="Name" Binding="{Binding Name}"/>
                            <DataGridTextColumn  Width="*" Header="Location" Binding="{Binding Location}"/>
                            <DataGridTextColumn  Width="auto" Header="Duration" Binding="{Binding Duration}"/>
                            <DataGridTextColumn  Width="auto" Header="Capacity" Binding="{Binding MaxCapacity}"/>
                            <DataGridTextColumn  Width="*" Header="Description" Binding="{Binding Description}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Second Tab -->
            <TabItem Header="Tours Today">
                <!-- Content of the second tab -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="toursTodayDataGrid"
                              Grid.Row="1"
                              Grid.ColumnSpan="2"
                              ItemsSource="{Binding ToursToday,UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedTour}"
                              SelectionMode="Single"
                              AutoGenerateColumns="False"
                              MouseDoubleClick="Tour_DoubleClick"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="*" Header="Picture">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImagesPath}" Width="50" Height="50"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Width="*" Header="Name" Binding="{Binding Name}"/>
                            <DataGridTextColumn  Width="*" Header="Location" Binding="{Binding Location}"/>
                            <DataGridTextColumn  Width="auto" Header="Duration" Binding="{Binding Duration}"/>
                            <DataGridTextColumn  Width="auto" Header="Capacity" Binding="{Binding MaxCapacity}"/>
                            <DataGridTextColumn  Width="*" Header="Description" Binding="{Binding Description}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
