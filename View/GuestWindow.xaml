<Window x:Class="BookingApp.View.GuestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="GuestWindow" Height="450" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:FirstImagePathConverter x:Key="FirstImagePathConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Name:" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0" Height="26" Width="44"/>
        <TextBox HorizontalAlignment="Left" Margin="42,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Width="103" Height="18" Grid.Column="0"  Text="{Binding AccommodationName, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Location:" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Height="26" Width="58" />
        <ComboBox Grid.Column="1" HorizontalAlignment="Left" Margin="58,0,0,0" VerticalAlignment="Center" Width="87" Height="22" SelectedIndex="{Binding LocationId, UpdateSourceTrigger=PropertyChanged}">
            <ComboBoxItem Content="Serbia, Novi Sad"/>
            <ComboBoxItem Content="Serbia, Beograd"/>
            <ComboBoxItem Content="Serbia, Smederevo"/>
            <ComboBoxItem Content="Serbia, Požega"/>
            <ComboBoxItem Content="Serbia, Kragujevac"/>
            <ComboBoxItem Content="Serbia, Niš"/>
            <ComboBoxItem Content="Serbia, Subotica"/>
            <ComboBoxItem Content="Serbia, Pančevo"/>
            <ComboBoxItem Content="Serbia, Čačak"/>
            <ComboBoxItem Content="Serbia, Užice"/>
        </ComboBox>
        <Label Content="Type:" Grid.Column="2"  HorizontalAlignment="Left" VerticalAlignment="Center" Height="26" Width="38"/>
        <ComboBox Grid.Column="2" Margin="36,0,2,0" VerticalAlignment="Center" Height="22" SelectedIndex="{Binding AccommodationType, UpdateSourceTrigger=PropertyChanged}">
            <ComboBoxItem Content="Apartament"/>
            <ComboBoxItem Content="House"/>
            <ComboBoxItem Content="Cottage"/>
        </ComboBox>
        <Label Content="Capacity:" Grid.Column="3"  HorizontalAlignment="Left" VerticalAlignment="Center" Height="26" Margin="3,0,0,0" Width="67"/>
        <TextBox Grid.Column="3" HorizontalAlignment="Left"  Margin="58,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Width="82" Height="18" Text="{Binding NumberOfPeople}"/>
        <Label Content="Number od days:" Grid.Column="4"  HorizontalAlignment="Left" VerticalAlignment="Center" Height="26" Width="100" Margin="10,0,0,0"/>
        <TextBox Grid.Column="4" HorizontalAlignment="Left"  Margin="115,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Width="40" Height="18" Text="{Binding NumberOfDays}"/>
        <Button Content="Search" Click="SearchAccommodation" Grid.Column="2" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Top" Width="90" Height="32" Margin="105,3,0,0"  Grid.RowSpan="2" Grid.ColumnSpan="2"/>
        <DataGrid Grid.Row="3" Grid.ColumnSpan="6" ItemsSource="{Binding Accommodations}" SelectionMode="Single"  SelectedItem="{Binding SelectedAccommodation}" AutoGenerateColumns="False" Margin="0,40,0,-5" IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick" HorizontalAlignment="Center" VerticalAlignment="Center">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="*" Header="Picture">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImagesPath, Converter={StaticResource FirstImagePathConverter}}" Width="auto" Height="auto"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn  Width="*" Header="Name"  Binding="{Binding Name}" />
                <DataGridTextColumn  Width="*" Header="Location" Binding="{Binding Location}"/>
                <DataGridTextColumn  Width="*" Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn  Width="*" Header="Capacity" Binding="{Binding Capacity}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
