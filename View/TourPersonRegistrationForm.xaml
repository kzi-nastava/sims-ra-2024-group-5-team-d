<Window x:Class="BookingApp.View.TourPersonRegistrationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="Person Registration Form" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        
        <!-- Content area for displaying person information -->
        <Grid Grid.Row="0" x:Name="ContentGrid">
            <!-- Content for displaying person details -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label x:Name="PersonNumberLabel" Content="Person 1" HorizontalContentAlignment="Center">
                </Label>
                <TextBlock Text="Full name:" />
                <TextBox x:Name="NameTextBox" Width="200" Margin="5" Text="" />
                <TextBlock Text="Age:" />
                <TextBox x:Name="AgeTextBox" Width="200" Margin="5" Text="" />
            </StackPanel>
        </Grid>

        <!-- Navigation buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="PreviousButton" Content="Previous" Width="100" Click="PreviousButton_Click" />
            <Button x:Name="NextButton" Content="Next" Width="100" Click="NextButton_Click" />
        </StackPanel>
    </Grid>
</Window>
  