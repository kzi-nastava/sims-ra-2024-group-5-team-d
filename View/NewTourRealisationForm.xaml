<Window x:Class="BookingApp.View.NewTourRealisationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="NewTourRealisationForm" Height="200" Width="400">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <!-- New row for buttons -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <Label Grid.Row="0" Content="Date and Time" HorizontalAlignment="Center" Margin="0 10 0 0"/>

        <!-- Date and Time Field -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBox Text="{Binding DateTime, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10">
            <Button Content="Confirm" Click="ConfirmTourRealisation_Button"  Width="80" Margin="5"/>
            <Button Content="Cancel" Click="CancelTourRealisation_Button" Width="80" Margin="5"/>
        </StackPanel>

        <!-- Other Content -->
        <!-- Add other content here if needed -->

    </Grid>
</Window>
