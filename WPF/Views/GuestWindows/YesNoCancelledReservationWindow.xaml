<Window x:Class="BookingApp.WPF.Views.GuestWindows.YesNoCancelledReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views.GuestWindows"
        mc:Ignorable="d"
        Title="YesNoCancelledReservationWindow" Height="100" Width="150" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Cancel reservation?"  
                Grid.Column="0"
                Height="auto" 
                Width="auto"
                FontSize="12" 
                Grid.ColumnSpan="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        <Button x:Name="YesCancelButton" 
                Content="Yes" 
                Grid.Row="1" 
                Grid.Column="0" 
                Width="50" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Click="YesCancelReservationButton"/>
        <Button x:Name="NoCancelButton" 
                Content="No" 
                Grid.Row="1" 
                Grid.Column="1" 
                Width="50" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Click="NoCancelReservationButton"/>
    </Grid>
</Window>
