<Window x:Class="BookingApp.WPF.Views.OwnerView.ShortcutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views.OwnerView"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="ShortcutsWindow" Height="450" Width="500">
    <ScrollViewer>
        <Grid Background="{StaticResource WhiteGrey}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ListView BorderThickness="0" 
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.CanContentScroll="False" ItemsSource="{Binding Shortcuts}" ItemContainerStyle="{StaticResource CustomListView}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border
                                CornerRadius="10"
                                 Height="50"
                                Width="460"  Background="{StaticResource WhiteWhite}" Margin="8 0 0 15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Left" Grid.Column="0" VerticalAlignment="Center" Text="{Binding Name}" Foreground="{StaticResource PrimaryBlack}" FontSize="20" FontWeight="Bold" Margin="10 0 0 0"/>
                                <Grid HorizontalAlignment="Right">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border CornerRadius="8"
                                            Height="36"
                                        Margin="0 0 20 0"
                                             Width="36" Background="{StaticResource PrimaryBlack}">
                                        <TextBlock  Text="Ctrl" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource WhiteWhite}"/>
                                    </Border>
                                    <Border CornerRadius="8"
                                            Height="36"
                                        Grid.Column="1"
                                         Margin="0 0 20 0"
                                             Width="36" Background="{StaticResource PrimaryBlack}">
                                        <TextBlock  Text="{Binding Command}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource WhiteWhite}"/>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button 
                                        Grid.Row="1"
                                        Background="Transparent" 
                                        FontSize="20" 
                                        Width="50"
                                        Height="50"
                                        FontWeight="SemiBold"
                                        BorderThickness="0"
            Template="{StaticResource CustomButtonTemplate}"
                                        VerticalAlignment="Bottom" Click="Close">
                <Border     CornerRadius="8" 
                                            Height="48"
                                            Width="48" Style="{StaticResource BorderHover}">
                    <TextBlock Text="OK" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource WhiteWhite}"/>
                </Border>
            </Button>
        </Grid>
    </ScrollViewer>
</Window>
