<Window x:Class="BookingApp.WPF.Views.OwnerView.ShowDetailedReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views.OwnerView"
        mc:Ignorable="d"
        WindowStyle="None"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"        
        xmlns:converter="clr-namespace:BookingApp.WPF.Converters"
        Title="ShowDetailsWindow" Height="600" Width="500">
    <Window.Resources>
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converter:FirstImagePathConverter x:Key="FirstImagePathConverter"/>
        <Style TargetType="Border">
            <Setter Property="Background" Value="{StaticResource WhiteWhite}"/>
        </Style>
        <Style TargetType="Grid">
            <Setter Property="Background" Value="{StaticResource WhiteGrey}"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="FontColor">
            <Setter Property="Foreground" Value="{StaticResource PrimaryBlack}"/>
            <Setter Property="FontFamily" Value="{StaticResource CustomFont}"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding OwnerRatingsViewModel.GuestName}" FontSize="32" FontWeight="DemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource FontColor}"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image  Source="C:\Users\lukai\Desktop\Resource\home.png" 
                                               Width="32" 
                                               Height="32"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"  Margin="0 0 10 0"/>
                <TextBlock Grid.Column="1" Text="{Binding OwnerRatingsViewModel.AccommodationName}" FontSize="16" FontWeight="DemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource FontColor}"/>
            </Grid>
            <Grid Grid.Column="1" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image  Source="C:\Users\lukai\Desktop\Resource\location.png" 
                                               Width="24" 
                                               Height="24"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" Margin="0 0 5 0"/>
                <TextBlock Grid.Column="1" Text="{Binding OwnerRatingsViewModel.Location}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource FontColor}"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Margin="30 10 30 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Cleanliness :" HorizontalAlignment="Left" FontSize="28" Style="{StaticResource FontColor}"/>
            <Grid Grid.Column="1" HorizontalAlignment="Left" Margin="10 0 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{Binding OwnerRatingsViewModel.CleanlinessRating}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24"/>
                <Image Grid.Column="1" Source="C:\Users\lukai\Desktop\Resource\star.png" 
                                               Width="32" 
                                               Height="32"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" Margin="5 0 0 0"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="3" Margin="30 0 30 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Correctness :" HorizontalAlignment="Left" FontSize="28" Style="{StaticResource FontColor}"/>
            <Grid Grid.Column="1" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{Binding OwnerRatingsViewModel.CorrectnessRating}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24"/>
                <Image Grid.Column="1" Source="C:\Users\lukai\Desktop\Resource\star.png" 
                                               Width="32" 
                                               Height="32"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" Margin="5 0 0 0"/>
            </Grid>
            <Grid Grid.Row="1" Grid.ColumnSpan="2" Visibility="{Binding IsSuggestedForRenovation,Converter={StaticResource BoolToVisibilityConverter}}" HorizontalAlignment="Center" Margin="10 0 0 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Renovation suggestions" FontSize="20" Style="{StaticResource FontColor}" HorizontalAlignment="Center" Margin="0 0 0 10"/>
                <Border Grid.Row="1" Background="{StaticResource WhiteWhite}" CornerRadius="8">
                    <TextBlock Text="{Binding RenovationText}" Padding="5" FontSize="20" Style="{StaticResource FontColor}" TextWrapping="WrapWithOverflow"/> 
                </Border>
            </Grid>
        </Grid>
        <Grid Grid.Row="4" Margin="10 0 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button BorderThickness="0" Background="Transparent" Command="{Binding BackwardCommand}">
                <Image Source="C:\Users\lukai\Desktop\Resource\back.png" Height="50"/>
            </Button>
            <ListView BorderThickness="0" 
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled"
                      Grid.Column="1"
                      Height="100"
                      x:Name="lista"
                      ItemsSource="{Binding ImagesPaths}"
                      Margin="5 0 5 0"
                      ScrollViewer.CanContentScroll="False">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource BorderHover}"
                                CornerRadius="2"
                                Width="91"
                                Height="90" Margin="0 0 0 0">
                            <Image Stretch="Fill" Source="{Binding}"/>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="10 0 5 0"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <Button Grid.Column="2" Background="Transparent" BorderThickness="0" Command="{Binding ForwardCommand}">
                <Image Source="C:\Users\lukai\Desktop\Resource\forward.png" Height="50"/>
            </Button>
        </Grid>
        <TextBlock Grid.Row="5" Text="Additional comment :" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="28" Margin="0 0 0 10" Style="{StaticResource FontColor}"/>
        <TextBox IsReadOnly="True" FontSize="20" Text="{Binding OwnerRatingsViewModel.Comment}" Grid.Row="6" Padding="10" Margin="20 0 20 0" TextWrapping="WrapWithOverflow" Background="{StaticResource WhiteWhite}" BorderThickness="0"/>
        <Button Margin="0 10 0 10" Grid.Row="7" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Click="Close" Template="{StaticResource CustomButtonTemplate}">
            <Border Style="{StaticResource BorderHover}" Padding="2">
                <TextBlock Text="OK" Foreground="{StaticResource WhiteWhite}" FontSize="24" Margin="5 0 5 0"/>
            </Border>
        </Button>
    </Grid>
</Window>
