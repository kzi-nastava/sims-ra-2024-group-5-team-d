<UserControl x:Class="BookingApp.WPF.Views.OwnerView.AccommodationStatsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:BookingApp.WPF.Views.OwnerView"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="572" d:DesignWidth="1108">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Stars Palic" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40"/>
        <TextBlock Text="Yearly stats" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="28"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Select year :" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            <ComboBox Grid.Column="1" Margin="10 0 0 0"  FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Center" SelectedIndex="0"  SelectedItem="{Binding SelectedYear, Mode=TwoWay}" ItemsSource="{Binding Years}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding ChangeStatsCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Cancelled,moved,finished reservations" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <lvc:CartesianChart Series="{Binding YearlyGeneralStats}" Grid.Row="1" LegendLocation="Top">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Godine" Labels="{Binding YearLabels}" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Broj rezervacija" />
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Percentage of reserved days" HorizontalAlignment="Center"  VerticalAlignment="Center"  FontSize="20"/>
                <lvc:PieChart Grid.Row="1"  LegendLocation="Top" Series="{Binding YearlyBussinessStats}">
                </lvc:PieChart>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Naslov" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <lvc:CartesianChart Grid.Row="1" Series="{Binding YearlyReccommendedrenovations}" LegendLocation="Top">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Year" Labels="{Binding YearLabels}" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis MinValue="0" />
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>

            </Grid>
        </Grid>
        <Button Content="Show reccommended actions" Grid.Row="4" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 20 0 50" Command="{Binding ShowReccommendationCommand}"/>
    </Grid>
</UserControl>
